EXESUFFIX=
OBJSUFFIX=.o
SLIBSUFFIX=.a
DYNLIBSUFFIX=.so

CC=clang -Wall -pedantic -ansi -std=c99 -lm
CCLIB=clang -fPIC -Wall -pedantic -ansi -std=c99 -lm
ARCMD=ar rcs
SHAREDCC=clang -Wall -shared
MAKE=gmake
CP=cp
MKDIR=mkdir -p
MKSTAGEARCHIVE=tar -czf packages/libstlio-amd64-freebsd.tar.gz -C stage .
RMDIR=rm -rf
RMFILE=rm
